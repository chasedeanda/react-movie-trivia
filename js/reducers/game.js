const initialState = {
    round: 1,
    player1Score: 0,
    player2Score: 0
}

const gameReducer = (state=initialState,action) =>{
    // switch(action.type){

    // }
    return state
}

export default gameReducer;